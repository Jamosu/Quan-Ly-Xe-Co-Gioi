# UI conventions

The frontend is React/Tailwind and partly hybrid mock/API. Reuse shared `DataTable`, filter, modal, audit, and action components. Keep business terms and enum mappings synchronized with the backend.

Master-data tables end with `Status`, `User`, and `Actions` columns in that order. See `.agents/rules/master-data-table-convention.md`.
